#include "main.h"

/**
 * times_table - creates a times tables
 *
 * Return: 0
 */

void times_table(void)
{
	int a, b;

	for (a = 0; b <= 10; num++)
	{
		_putchar ('0');
		for (b = 1; b <= 9; b++)
		{
			a = i * j;
			if a <= 9;
			{
				putchar(a + '0');
				putchar(',');
				putchar(' ');
			}
			else
			{
				putchar((a / 10) + '0');
				putchar((a % 10) + '0');
				putchar(',');
				putchar(' ');
			}
		putchar('\n');
		}
		retur 0;
	}
}
}
