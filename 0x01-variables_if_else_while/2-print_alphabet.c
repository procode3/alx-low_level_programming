#include <stdio.h>

/**
 * main - Prints characters of the alphabet in lowercase
 *
 * Return: Alphabets or nothing
 */

int main(void)
{
	for (int a = 'a'; a <= 'z'; a++)
	{
		putchar(a);
	}
	return (0);
}
